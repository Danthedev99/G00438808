import { ComponentFixture, TestBed } from '@angular/core/testing';
import { G00438808Page } from './g00438808.page';

describe('G00438808Page', () => {
  let component: G00438808Page;
  let fixture: ComponentFixture<G00438808Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(G00438808Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
